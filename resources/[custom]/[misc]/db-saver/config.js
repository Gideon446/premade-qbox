const config = {
    webhook: "",
    time: 60, // in min

    connection: {
        host: "localhost",
        user: "root",
        password: "",
        database: "qboxframework_1515c6",
    },

    embed: {
        color: "GREEN"
    }
}

module.exports = config