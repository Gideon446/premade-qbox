const config = {
    webhook: "",
    time: 60, // in min

    connection: {
        host: "localhost",
        user: "root",
        password: "",
        database: "hrpqbox",
    },

    embed: {
        color: "GREEN"
    }
}

module.exports = config